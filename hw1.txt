%{
/* Declerations section */
#include <stdio.h>
void showToken(char *);
%}

%option yylineno
%option noyywrap

whitespace	([ \t\n\r])
digit	([0-9])
letter	([a-zA-Z])
printable ([!-~])|{whitespace}

%%


%%

void showToken(char * name){
  printf("Token is %s\n", name);
  printf("The lexeme is %s\n", yytext);

}
